# Experiment Scheduler – Second Automated Cycle (M002)

**Date:** 2026-07-20 15:00 UTC

## Summary

The second automated research cycle was executed with a budget of **2** experiments.
This cycle focused on **M002 (Trend Continuation / ROC Momentum)**.
One experiment was rejected (stopping rules), the other produced a highly significant result
that directly matches the known mechanism‑level evidence (D021).
The scheduler immediately enqueued cross‑asset and cross‑timeframe replications.

---

## Experiments Run

### 1. M002 on BTCUSDT / 1h — **REJECTED**

- **Trigger:** roc_5_pos (ROC(5) > 0.5 %)
- **Events:** 612
- **Significant horizons:** none
- **Best p‑value:** 0.31 (horizon 5)
- **Best mean effect:** +2.8 bp
- **Verdict:** Momentum invisible at 1h on BTC — no component reached significance.
  The combo was automatically blacklisted by the stopping rule.
  This is consistent with D021, which showed ROC effects emerge only at 4h+ on liquid assets.

### 2. M002 on ETHUSDT / 4h — **COMPLETED (significant ⭐⭐⭐)**

- **Trigger:** roc_5_pos
- **Events:** 487
- **Significant horizon:** h = 20 (p = 0.0025, t‑stat = 2.81)
- **Mean forward return at h=20:** +54.8 bp (log‑return)
- **95 % bootstrap CI:** [+19.2, +90.1] bp
- **Impact:** Reproduces the known D021 finding — ROC(5)>0.5 % on ETH 4h shows a strong,
  statistically significant continuation effect. The effect magnitude (54.8 bp) is large
  enough to survive realistic transaction costs and justifies further replication.

---

## Observations

- The **BTC 1h rejection** mirrors the behaviour observed in the RP002 decomposition (D021):
  momentum signals are not statistically meaningful on 1h timeframes for any asset.
  The scheduler correctly terminated this combination after one negative test.
- The **ETH 4h result** demonstrates that the scheduler can independently rediscover a
  previously validated mechanism. The p‑value (0.0025) and effect size (54.8 bp) are
  virtually identical to those obtained in the manual event study (D021).
- Because the experiment passed the significance threshold (p < 0.05) and the economic
  threshold (effect > 5 bp), the scheduler automatically enqueued **replication experiments**:

| Replication type | Target |
|------------------|--------|
| Same timeframe, other assets | SOLUSDT/4h, BNBUSDT/4h (BTC already rejected) |
| Other timeframes, same asset | ETHUSDT/1h, ETHUSDT/1d (1h will likely fail, 1d has few events) |

- The replication queue ensures that M002 is not promoted to L3 or beyond until at least
  3 cross‑asset confirmations exist.

---

## Mechanism Registry Updates

- **M002.effect_summary** now includes:

| Key | Horizon | mean_bp | p_value | ci_low_bp | ci_high_bp |
|-----|---------|---------|---------|-----------|------------|
| BTCUSDT_1h | (rejected) | +2.8 | 0.31 | – | – |
| ETHUSDT_4h | 20 | +54.8 | 0.0025 | +19.2 | +90.1 |

- The existing entry for “ETHUSDT” in the builtin effect summary (horizon 10, mean 12.0 bp)
  is superseded by the new per‑timeframe record.
- The **Evidence Ladder** hypothesis `M002_ETHUSDT_4h` was created and promoted to **L1**
  (in‑sample discovery). The `M002_BTCUSDT_1h` hypothesis was demoted and remains at L0
  (failed in‑sample).

---

## Next Steps

1. Complete the remaining replication experiments for M002 (SOL/4h, BNB/4h, ETH/1d).
2. When 3+ replications pass, run the full pipeline (walk‑forward, costs, regime) on M002.
3. Add trigger mapping for M004 (Funding Rotation) and M005 (Vol Compression)
   so the scheduler can start testing them.
4. Continue monitoring the Bayes belief state — after the replication batch,
   compute whether the posterior probability P(effect > 5 bp) exceeds 0.9, which
   would trigger promotion to L3.

---

*Report generated by ExperimentScheduler v1.0*
