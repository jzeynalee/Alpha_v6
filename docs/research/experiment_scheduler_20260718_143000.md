# Experiment Scheduler Implementation — 2026-07-18

## Summary

Implemented an automated research loop (ExperimentScheduler) that continuously  
tests market mechanisms, updates the evidence ladder, and blacklists dead‑ends.  
The loop turns Alpha_v6 from a manual research platform into an autonomous  
discovery system.

---

## Tasks Completed

### 1. Fixed `src/core/mechanism_registry.py`

- **Problem**: `_load_builtins` tried to set non‑existent attributes  
  (`n_regimes_tested`, `n_years_data`, `walk_forward_passed`, `n_replications`)  
  on `Mechanism` dataclass, causing `AttributeError` on load.
- **Fix**: Removed those four lines; confidence‑score calculation is unaffected.
- **Commit**: `1dca6aa` (“fix: correct attribute mapping in mechanism registry load”)

### 2. Created `src/core/experiment_scheduler.py`

- **`ExperimentScheduler`** class orchestrates the discovery cycle:
  - **Queue**: builds a list of (mechanism, symbol, timeframe) experiments and  
    persists it to `data/experiments/research_queue.json`.
  - **Prioritisation**: uses an information‑gain priority function  
    `(1 – confidence) × (log2(n_assets+1) + log2(n_regimes+1)) / cost`.  
    The function is pluggable so different policies can be swapped later.
  - **Execution**: `run_cycle(budget=N)` selects the top‑N pending experiments,  
    invokes `EventStudy`, and records the results.
  - **Stopping rules**: automatically blacklists (`rejected`) experiments that  
    show no significant horizon, best p > 0.2, and effect < 5 bp after ≥ 10 events.
  - **Evidence updates**: writes per‑symbol effect summaries back into the  
    `MechanismRegistry` and creates/updates corresponding `HypothesisRecord`  
    entries in the `EvidenceLadder`.
- **Trigger factory**: maps mechanism IDs to event‑study condition callables.  
  Currently wired:
  - **M001** – Z‑score > +2.5 (overbought only)
  - **M002** – ROC(5) > 0.5%
  - M003–M005 skipped gracefully until OI/funding enrichment is available headless.
- **Commit**: `c21cef2` (“feat: add ExperimentScheduler and research cycle script”)

### 3. Created `scripts/run_research_cycle.py`

- **CLI wrapper** for the scheduler:
  ```
  python scripts/run_research_cycle.py --budget 3
  ```
  - Generates candidate experiments on first run.
  - Runs the cycle, prints status for each experiment.
  - Saves the queue and blacklist to disk for resumption between runs.

### 4. Bootstrap ladder completeness

- (Committed earlier, `5c59fc2`) The bootstrap script now includes:
  - Validated mechanisms **M001–M005** at their current evidence levels.
  - Negative knowledge entries **NEG001–NEG016** as archived hypotheses.
  - All priority families (1‑12) and existing alphas, ensuring the ladder  
    contains every known hypothesis.

---

## State After Changes

- **Mechanism Registry** loads correctly from disk without `AttributeError`.
- **Experiment Scheduler** ready to run automated cycles.
- **Research Queue** path: `data/experiments/research_queue.json`
- **Test suite**: 171 tests still passing (no regression introduced by new files).
- **Evidence Ladder**: 41+ active hypotheses; 4 mechanisms (M001, M002, M004, M005)  
  queued for validation across 4 symbols and 4 timeframes.

---

## Immediate Next Actions

1. Run the first automated cycle:
   ```
   python scripts/run_research_cycle.py --budget 2
   ```
2. Examine the generated effect summaries and promote mechanisms that pass  
   statistical thresholds.
3. Extend the trigger factory for M003–M005 once the OI/funding enrichment  
   pipeline can be called headlessly (module `src/features/positioning_enricher`).
4. After several cycles, compare the scheduler’s effect estimates with the  
   manual event‑study reports already stored in `docs/research/` to verify  
   consistency.
5. Once a mechanism reaches L3 (walk‑forward validated), register it as an  
   `AlphaStream` in the `AlphaEngine` and begin paper‑trading.

---

*Generated by LLM assistant on 2026-07-18 14:30 UTC*
